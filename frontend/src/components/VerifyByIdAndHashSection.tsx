import type { JSX } from "react";

/** Перевірка за ID + documentHash (каркас). */
export function VerifyByIdAndHashSection(): JSX.Element {
  return <section aria-label="Verify by id and hash">Перевірка за ID і хешем (у наступному кроці)</section>;
}
