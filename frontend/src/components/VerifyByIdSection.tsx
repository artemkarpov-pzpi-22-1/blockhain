import type { JSX } from "react";

/** Перевірка за certificateId (каркас). */
export function VerifyByIdSection(): JSX.Element {
  return <section aria-label="Verify by id">Перевірка за ID (у наступному кроці)</section>;
}
