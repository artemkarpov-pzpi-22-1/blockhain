import type { JSX } from "react";

/** Форма реєстрації сертифіката (буде підключена до контракту). */
export function RegisterCertificateForm(): JSX.Element {
  return <section aria-label="Register certificate">Реєстрація (у наступному кроці)</section>;
}
