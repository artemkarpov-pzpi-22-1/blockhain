import type { JSX } from "react";

/** Відкликання сертифіката (каркас). */
export function RevokeCertificateSection(): JSX.Element {
  return <section aria-label="Revoke certificate">Відкликання (у наступному кроці)</section>;
}
